# EDF Yocto workspace (pulled by repo sync, huge)
edf/

# Yocto shared caches
sstate-cache/
downloads/

# Build output artifacts
output/

# PetaLinux installer (legacy, no longer needed for EDF)
docker/*.run
